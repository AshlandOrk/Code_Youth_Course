// Exercise
// Create a function that returns the area of a given rectangle

const rectW = 13;
const rectH = 20;

rectArea(rectH, rectW);

function rectArea(a, b) {
  let area = a * b;
  console.log("The area of the rectangle is " + area + " cm-squared across.");
}
